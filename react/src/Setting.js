// Use this file to store all the environment variables
// Add header by import GLB from "./config.js"
// Access with GLB.backend => "http://127.0.0.1:4000"

const url = "127.0.0.1";
const port = "4000";

export default {
    backend: (`http://${url}:${port}`)
};